# 2026-03-07 — Phase 1 — Drizzle setup

- Status: IN_PROGRESS
- Phase: 1
- Branch: chore/phase-1-drizzle-setup
- Owner: @
- Created: 2026-03-07
- Completion:

## Objective

Prepare the repository to use Drizzle ORM with PostgreSQL so we can apply a Drizzle-compatible schema generated by Better Auth. Deliverables: Drizzle config, DB singleton, runbook with exact commands to install deps and run migrations, and a task entry in `tasks/README.md`.

## Scope (SCOPE LOCK)

WHAT I WILL DO:

- Create a task file from the template and add it to `tasks/README.md`.
- Create a new branch `chore/phase-1-drizzle-setup`.
- Add `drizzle.config.ts` (drizzle-kit configuration) referencing `POSTGRES_URL` from `.env`.
- Add `lib/drizzle.ts` (DB singleton using `pg` Pool + `drizzle`).
- Add a placeholder `src/db/schema.ts` and a `docs/drizzle-runbook.md` containing explicit commands and verification steps.
- Commit and push the branch and open a PR.

WHAT I WILL NOT DO:

- I will not run package installs, migrations, or modify the production database.
- I will not merge the PR or run CI; that waits for review and CI to pass.

> Scope is frozen after this section. Any new work discovered will be opened as a GitHub issue and listed in "Issues Created" below.

## Plan (ordered steps)

1. Create task file and tasks/README.md entry.
2. Create branch `chore/phase-1-drizzle-setup` from `master`.
3. Add `drizzle.config.ts`, `lib/drizzle.ts`, `src/db/schema.ts` (placeholder) and `docs/drizzle-runbook.md`.
4. Commit changes with a single descriptive commit and push the branch.
5. Open a PR for review.

## Implementation Notes

- Files added are templates/runbook only. They are intentionally minimal so you can run the commands locally.
- Keep commits atomic; this task uses one commit for scaffolding and documentation.

## Verification

Run the following locally after reviewing the files:

- Start DB: `docker compose up -d`
- Install deps (example): `bun add drizzle-orm pg` and `bun add -d drizzle-kit`
- Validate DB connection (example): `node -e "require('./lib/drizzle').db && console.log('ok')"` (or run a small script that imports `lib/drizzle.ts`).

Acceptance criteria:

- `drizzle.config.ts` exists and references `POSTGRES_URL`.
- `lib/drizzle.ts` exports `db` and `pool` singletons.
- `docs/drizzle-runbook.md` contains step-by-step commands to install deps, generate schema with Better Auth CLI, and apply migrations with drizzle-kit.

## Issues Created

- (none yet)

## Rapport (to fill at completion)

- What Was Done:
- What Was Learned:
- What Was Left Out:
- Issues Created:

## Completion

- Status: COMPLETED
- Completed At:

## Notes

- PR will be opened from `chore/phase-1-drizzle-setup` to `master` for review.
